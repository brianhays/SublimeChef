<snippet>
	<content><![CDATA[
cookbook_file '${1:name}' do
  source '${2:source}'
  owner '${3:root}'
  group '${4:root}'
  mode ${5:00644}
  action :${6:create}
end

]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>cookbook_file</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.ruby</scope>
	<description>cookbook_file</description>
</snippet>
